package schemer

import (
	"testing"
)

func TestDecodeArray1(t *testing.T) {

	/*
		var ArraySchema := [5]float32{10.1, 20.2, 30.3, 40.4, 50.5}
		//var floatSlice := [] := make([]float64, 10)

		bSchema := BoolSchema{WeakDecoding: false}

		var buf bytes.Buffer
		var err error
		var valueToEncode bool = false
		buf.Reset()

		fmt.Println("bool to bool")

		bSchema.Encode(&buf, valueToEncode)
		if err != nil {
			t.Error(err)
		}
	*/

}
